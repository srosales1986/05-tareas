import { FiltrarTerminadasPipe } from './filtrar-terminadas.pipe';

describe('FiltrarTerminadasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarTerminadasPipe();
    expect(pipe).toBeTruthy();
  });
});
