import { PendVsTermPipe } from './pend-vs-term.pipe';

describe('PendVsTermPipe', () => {
  it('create an instance', () => {
    const pipe = new PendVsTermPipe();
    expect(pipe).toBeTruthy();
  });
});
